# Drivers Layer

This directory is reserved for CubeMX-generated MCU-specific drivers such as:

- `adc.*`
- `tim.*`
- `usart.*`

The repository does not yet contain the full generated STM32 project, so this
directory currently documents the intended ownership only.
