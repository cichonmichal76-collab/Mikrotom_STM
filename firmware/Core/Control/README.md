# Control Layer

Target module ownership:

- `motorState.*`
- `foc.*`
- `pid.*`
- `trajectory.*`

Responsibilities:

- current and position runtime updates
- FOC and PID control path
- trajectory generation
