# App Layer

Target module ownership:

- `axis_state.*`
- `axis_control.*`
- `commissioning.*`

Responsibilities:

- own system state transitions
- validate operator-level commands
- expose commissioning workflow
